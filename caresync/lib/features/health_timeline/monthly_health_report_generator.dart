// Legacy shim: re-export the report generator from the `reports` feature so
// older imports referencing the health_timeline location keep working.
export 'package:caresync/features/reports/monthly_health_report_generator.dart';
